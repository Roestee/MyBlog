@model MyService

<div class="row justify-content-evenly">
    @{
        foreach (var service in Model.Services)
        {
            await Html.RenderPartialAsync("Components/_AdminLayoutServiceUpdate/Default", service);
        }
    }
</div>